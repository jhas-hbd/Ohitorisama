<div class="container">
  <div class="row">
    <div class="col">
      <h4 class="my-5">プラン一覧</h4>
      <div class="row">
        <div class="col-md-4 text-center">プランID</div>
        <div class="col-md-4 text-center">ユーザーネーム</div>
        <div class="col-md-4 text-center">タイトル</div>
      </div>
      <% @plans.each do |plan| %>
        <div class="row my-4">
          <div class="col-md-4 text-center"><%= plan.id %></div>
          <div class="col-md-4 text-center"><%= plan.user.name %></div>
          <div class="col-md-4 text-center">
            <%= link_to admin_plan_path(plan) do %>
              <%= plan.title %>
            <% end %>
          </div>
        </div>
      <% end %>
      <div class="row justify-content-center my-5">
        <%= paginate @plans %>
      </div>
    </div>
  </div>
</div>