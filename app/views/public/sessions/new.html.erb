<div class="container page">
  <div class="row pt-5">
    <div class="col-md-8 mx-auto">
      <h3 class="my-4">ログイン</h3>
      <h4 class="mb-4">会員の方はこちらからログイン</h4>

      <%= form_with model: @user, url: user_session_path do |f| %>

        <div class="row">
          <div class="col-md-4"><label>メールアドレス</label></div>
          <div class="col-md-4 text-right"><%= f.email_field :email, autofocus: true, autocomplete: "email" %></div>
        </div>

        <div class="row">
          <div class="col-md-4"><label>パスワード</label></div>
          <div class="col-md-4 text-right"><%= f.password_field :password, autocomplete: "current-password" %></div>
        </div>

        <% if devise_mapping.rememberable? %>
          <div class="row">
            <div class="col-md-4">
              <%= f.check_box :remember_me %>
              <%= f.label :remember_me %>
            </div>
          </div>
        <% end %>

        <div class="row">
          <div class="col-md-4"></div>
          <div class="col-md-4 text-right"><%= f.submit "ログイン", class: "btn btn-sm btn-outline-dark" %></div>
        </div>
      <% end %>

      <div class="mt-5">
        <h4>会員登録がお済みでない方</h4>
        <%= render "public/shared/links" %>から新規登録を行ってください。
      </div>

    </div>
  </div>
</div>