<div class="container page">
  <div class="row pt-5">
    <div class="col-md-8 col-10 mx-auto">
      <h3 class="my-4">ログイン</h3>
      <h4 class="mb-4">会員の方はこちらからログイン</h4>

      <%= form_with model: @user, url: user_session_path do |f| %>
        <div class="field d-md-flex mb-3">
          <%= f.label :email, "メールアドレス", class: "col-md-6" %><br />
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "col-md-6" %>
        </div>

        <div class="field d-md-flex mb-3">
          <%= f.label :password, "パスワード", class: "col-md-6" %><br />
          <%= f.password_field :password, autofocus: true, autocomplete: "current-password", class: "col-md-6" %>
        </div>
        
        <div>
          <% if devise_mapping.rememberable? %>
            <div class="field d-md-flex mt-3">
              <div class="col-md-6"></div>
              <div class="col-md-6">
                <%= f.check_box :remember_me %>
                <%= f.label :remember_me %>
              </div>
            </div>
          <% end %>
        </div>

        <div class="text-center my-5">
          <%= f.submit "ログイン", class: "btn btn-outline-dark" %>
        </div>
        
      <% end %>

      <div class="text-center my-5">
        <h4>会員登録がお済みでない方</h4>
        <%= render "public/shared/links" %>から新規登録を行ってください。
      </div>

    </div>
  </div>
</div>